# Roadmap operativo de presuntamente

> **Estado vivo del proyecto.** Este es el primer fichero que cualquier persona (o agente LLM) debe leer al empezar a trabajar, y el último que debe actualizar al cerrar la sesión. Si abres el proyecto por primera vez o desde otra máquina, empieza aquí.
>
> El roadmap **conceptual** — razonamiento de las fases, criterios de salida, esfuerzo estimado — vive en [`docs/diseno/06-roadmap-por-fases.md`](docs/diseno/06-roadmap-por-fases.md). Este fichero es la versión **operativa**: dónde estamos, qué hay encima de la mesa, qué se ha aprendido.

**Última actualización:** 2026-05-22 (sesión 6 + sesión paralela) — **PR3 del caso Begoña Gómez + PR1, PR2 y PR3 del caso González Amador entregados en main**. PR3 González Amador (commit único conforme a nueva norma de granularidad) amplía V-11 del schema `rol-en-caso` para admitir organizaciones en roles imputadores (LO 5/2010 de responsabilidad penal de personas jurídicas) y sube a N1 del BOE el cambio de juez, además de corregir la pieza Quirón (instruida por JI 19, no AN) y añadir el encargo del informe UCO. Cambio también en AGENTS.md: dos normas nuevas — "Repositorio multiagéntico en paralelo" (clasificar `git status`, stagear por ruta explícita, verificar staging antes del commit) y "Granularidad de commits" (un commit por sesión coherente, no por sub-bloque). La sesión paralela del segundo agente arranca el caso del JI nº 19 de Madrid contra Alberto González Amador (pareja de Isabel Díaz Ayuso) por presunto fraude fiscal y falsedad documental sobre Maxwell Cremona S.L. (perjuicio AEAT 350.951 €, ejercicios 2020-2021). PR1: caso + 9 personas + 10 organizaciones nuevas + 9 documentos + 7 hitos + 5 hechos + 14 roles + nuevo delito `delito-contra-hacienda-publica` catalogado (art. 305 CP). Reaplicado el patrón `investigado → desimputado` con el asesor fiscal Javier Gómez Fidalgo (sobreseído provisional el 29-may-2025) y la secuencia `cambio_juez` con Inmaculada Iglesias → Antonio Viejo. PR2: pieza separada de la rama Quirón ante la Sección Segunda de la Sala de lo Penal de la AN con detención de David Herrera Lobato en Arahal (20-ene-2026); nueva organización Quirón Prevención S.L.U.; escrito de defensa del 19-feb-2026 invocando la condena del FGE García Ortiz como vulneración del proceso justo. Total tras ambos PRs: 194 YAML validados, `astro check` 0/0/0, `pnpm build` 77 páginas con la ficha del caso renderizada en `/casos/gonzalez-amador`. **Sin push** (el maintainer decide cuándo).

**Anterior (sesión 6):** **PR3 del caso Begoña Gómez entregado en main**. Incorpora Juan José Güemes como persona desimputada (presidente del Centro Internacional de Gestión Emprendedora de IE Business School, ex consejero de la Comunidad de Madrid bajo Aguirre) con persona, dos roles consecutivos (investigado 18-nov-2024 → desimputado 16-may-2025), dos hitos propios (imputación + recurso Fiscalía contra la imputación 03-dic-2024) y la ampliación del hito de desimputación de Goyache para reflejar que el mismo auto AP Madrid desimputaba a ambos. Suma además tres hitos posteriores al 21-abr-2026: ofrecimiento de personación a la UCM por el JI 41 el 03-oct-2025 (que reasigna el `hito_origen_id` del rol ucm-perjudicada a un hito propio); escrito de conclusiones provisionales de la defensa de Begoña Gómez del 18-may-2026 (pide absolución, propone más de 30 testigos incluyendo a Goyache, Escassi de Google, Rosauro Varo y directivos de Reale/La Caixa/Indra/Telefónica); y desestimación del 22-may-2026 del recurso de reforma de la defensa contra la providencia del 5-may. **Cambio de schema** en `hito.schema.json` para añadir el nuevo `tipo: escrito_conclusiones_provisionales` (con label en `lib/labels.ts`), consistente con `comparecencia_congreso` / `publicacion_investigacion_periodistica`. Acumulado: 6 personas + 15 organizaciones + 22 documentos + 13 hitos + 8 hechos + 17 roles. 166 YAML validados, `astro check` 0/0/0, build 77 páginas. Sin push.

**Anterior (sesiones 5d–5h):** Header responsive consolidado + hero/logo retocados tras el catálogo de delitos:

- **5d — Scroll horizontal eliminado en zona intermedia.** Breakpoint de colapso subido de 720 → 1080px (umbral donde el grid desktop `auto 1fr auto auto` deja de caber con 6 items en el nav). `flex-wrap: wrap` sustituye al `overflow-x: auto` del nav colapsado: 2 líneas en lugar de scrollbar feo. Verificado a 1440/1280/1100/1024/900/720/500/360px sin scroll horizontal.
- **5e — Subrayado nav pegado al texto.** `align-items: center` + padding vertical en el `<a>` (cambia altura natural de 78px → ~41px); `::after { bottom: 4px }` queda bajo el texto y separado del trim tricolor. Nav colapsado pasa a `padding: 14px; font-size: 13px` (alineado con desktop).
- **5f — Menú hamburguesa <720px.** Botón ☰ junto a la lupa, panel navy desplegable con los 6 links verticales + ES/CAT al final. JS por event-delegation desde `document` → sobrevive a los swaps del `ClientRouter`. Cerrado por click/ESC/click-fuera/link/`astro:before-preparation`. Tres rangos finales: ≥1080 desktop · 720–1080 dos filas · <720 hamburguesa.
- **5g — Animación slide-down panel + nav 2 filas más grande.** Panel deja `hidden` por `data-open` + CSS `max-height`/`opacity`/`visibility`, 320ms apertura / 280ms cierre, `prefers-reduced-motion: reduce` desactiva. Nav 2 filas sube de `padding: 14px; font-size: 13px` a `padding: 18px; font-size: 14px` por feedback ("la fila de abajo sigue súper pequeña").
- **5h — Hero kicker + logo sin fondo + brand cleanup + hover nav full-height.** Kicker del hero `PgInicio` pasa de "Sin ruido, sin spin, sin política" a "sin ruido, sin ideología, sin financiación" (gira el énfasis del tratamiento sin cuota política al modelo de sostenimiento). `branding/logo.png` con fondo eliminado (transparente, encaja sobre la banda navy y otros). Filete mostaza decorativo bajo el brand (`.site-brand::after`) eliminado — brand block más limpio, decoración concentrada en banda navy + trim tricolor. Refactor del nav: texto envuelto en `.site-nav__label` para que el subrayado viva en el span (siempre 4px del baseline) mientras el `<a>` recupera `align-items: stretch` y ocupa toda la altura del header → hover background full-height. Typo "idiología → ideología" corregido.

**Anterior (sesión 5c):** Catálogo de delitos publicado. Rutas `/delitos` (listado tabular con filtros texto + familia) y `/delitos/[slug]` (ficha en 4 secciones: marco normativo con chips de artículos CP y link al BOE, descripción, casos, atribuciones). Mapping `familiaDelitoLabel()` en `lib/labels.ts` para los 8 valores del enum. Nav "Delitos" cableado en `BaseLayout.astro` entre Organizaciones y Biblioteca. Flag `enableDelitos: true` en `RichProse.astro` — verificado con 20 autoenlaces en la ficha de Begoña Gómez. 116 YAML, astro check 0/0/0, build 54 páginas.

---

## Estado actual

- **Fase activa**: Fase 0 ✅ · Fase 1.0 (integración del design system) ✅ · Fase 1 MVP Plus Ultra (PR1 + PR2) ✅. **Fase 2 en marcha** con Begoña Gómez PR1 + PR2 + PR3 ✅ y **González Amador PR1 + PR2 + PR3 ✅** (sesión paralela del segundo agente, en main).
- **Último hito**: PR3 del caso González Amador en main (sesión paralela 2026-05-22). PR3 **amplía V-11** para admitir organizaciones en roles imputadores conforme a la LO 5/2010 (mismo patrón que la ampliación con `perjudicado` de Begoña PR2), **sube a N1 del BOE** el cambio de juez (RD 838/2025 + Acuerdo CGPJ 13-may-2025), **corrige** la pieza Quirón como instruida por el JI 19 (no AN), y añade un hito de encomienda a la UCO del 27-jun-2025 con cobertura del retraso documentado en mayo de 2026 por Público.es. Caso González Amador acumulado tras PR3: **9 personas + 11 organizaciones nuevas + 16 documentos (2 N1 BOE) + 9 hitos + 9 hechos + 15 roles + Maxwell Cremona como persona jurídica procesada**. 204 YAML validados, `astro check` 0/0/0. Sin push.
- **Próximo paso comprometido**: Begoña Gómez PR4 si aparecen fuentes oficiales N1 / nuevos hitos procesales; González Amador PR4+ si aparece auto de la AP Madrid 7-nov-2025 en CENDOJ o se entrega el informe UCO; o siguiente caso adicional del backlog (Koldo · Cerdán · Fiscal General). Lo que el maintainer prefiera priorizar.
- **Dev server local**: `pnpm dev` en `http://localhost:4321` (config en [`.claude/launch.json`](.claude/launch.json)).
- **Workflow git**: **commits directos a `main`**, sin ramas ni PRs, decidido por el maintainer el 2026-05-21 mientras dure el MVP. Detalle en [`AGENTS.md` §"Workflow de rama y PRs"](AGENTS.md). PR #2 (Lote 1 de integración) es el último PR formal mientras esta política esté activa.

---

## Backlog inmediato

Una idea, un bullet. `[ ]` pendiente, `[x]` hecho. Items completados se eliminan tras una o dos sesiones (la historia detallada está en el git log).

### Antes de Fase 1
- [x] Configurar git con email noreply.
- [x] Commit y push inicial del scaffolding.
- [x] Lenguaje visual canónico en [`/DESIGN.md`](DESIGN.md) (formato esperado por Claude Design + Claude Code).
- [x] Sistema de diseño completado por Claude Design e importado al repo como skill local en `/.claude/skills/presuntamente-design/` (README + SKILL.md + `colors_and_type.css` + 30+ previews HTML + UI kit JSX). Wordmarks SVG en `/public/branding/`. Inconsistencia "rojo=político" corregida a "rojo=semántico" en README y SKILL del bundle para alinearlos con DESIGN.md.

### Integración del design system — Fase 1.0

Trabajando en rama `fase-1/integrar-design-system` ([PR #2](https://github.com/davidchicano/presuntamente/pull/2)). 5 lotes; Lote 1 entregado, Lotes 2-5 pendientes.

**Lote 1 — Tokens + chrome + split landing/catálogo** ✅
- [x] Portar `colors_and_type.css` del bundle a `/src/styles/global.css`. Open Props se mantiene como capa base.
- [x] `PresuntamenteLogo.astro` con tres variantes (wordmark+isotipo, wordmark, isotipo) e `inverse`. **Decisión:** el wordmark se renderiza como texto (`presuntamente.org`, Gill Sans / Lato 22px navy), NO como `<img>` del SVG — el `wordmark.svg` del repo es un placeholder roto (`<text>` sin font-family ni fill). El SVG sigue en `/branding/` para favicon / social cards cuando se reemplace por una versión outlined.
- [x] `BaseLayout.astro` rehecho con chrome ministerial completo: banda navy con doble trim navy+mostaza (`::after` linear-gradient), bloque blanco con trim mostaza interno tipo "placa sellada", nav/lang con subrayado mostaza animado izq→der (180ms), sub-strip de breadcrumb, footer 4 columnas con filete tricolor `#8e2424`/mostaza/`#8e2424` muted + eco al 4-5%.
- [x] Split de rutas: `/` → `PgInicio` (landing institucional con hero + cifras del inventario + 4 secciones + meta de redacción + disclaimer) · `/casos` → `PgCasos` (catálogo tabular con `page-id`, `sec-h` numeradas, `filter-bar` deshabilitada hasta el Lote 2, `.tbl` con 1 fila hardcoded de Plus Ultra). Carpetas `/src/pages/cat/casos/` paralelas con `.gitkeep`.
- [x] Mobile-friendly: en `<720px` el header pasa a 2 filas (brand+lang arriba, nav scrollable abajo); `.page-id` y `.filter-bar` apilan vertical; subtítulo institucional se oculta.
- [x] Confirmada skill `/presuntamente-design` invocable (`user-invocable: true` en SKILL.md del bundle).

**Lote 2 — Content Collections + lectura YAML real** ✅
- [x] Configurar Astro Content Collections sobre `/content/`: `casos`, `personas`, `organizaciones`, `documentos`, `delitos`, `hitos` (anidados en `casos/<slug>/hitos/`), `hechos` (anidados), `roles` (anidados). Definidas en [`src/content.config.ts`](src/content.config.ts) con Zod schemas mínimos viables + `.passthrough()` para no duplicar los JSON Schemas de `/schemas/`.
- [x] Cablear contadores hardcoded de `PgInicio` (cifras del inventario: 1/2/7/3) y la fila hardcoded de `PgCasos` a las collections reales. La tabla de casos lee `casos`, deriva el último hito de cada caso por fecha máxima en su carpeta `/hitos/`, cuenta personas con rol de imputación activo (investigado/procesado/acusado/condenado sin `fecha_fin`) y resuelve el órgano judicial al primer acrónimo de `nombres_alternativos`.
- [x] Activar filtros de `PgCasos` — búsqueda por texto sobre nombre mediático + nombre oficial + órgano (incluido acrónimo) + nº de procedimiento, filtro por fase (instr / juicio / firme), orden por último hito ↓ o por implicados ↓. Vanilla JS sobre `data-*` attributes (sin frameworks reactivos); empty state "Sin resultados para los filtros actuales." cuando ninguna fila pasa.

**Lote 3 — `PgCasoDetalle` con ficha completa de Plus Ultra** ✅
- [x] Componentes compartidos creados en `/src/components/`: `SectionH`, `LevelBadge`, `PhaseBadge`, `EpistemicBadge`, `CalGlyph`, `Cite`, `OrgGlyph`, `PersonaCard`, `OrgCard`, `Hito`, `Hecho`, `DocumentoCard`. Módulo `/src/lib/labels.ts` con mappings enum→label centralizados (preparado para i18n con firma `_lang`). Migrados desde `preview/comp_*.html`.
- [x] Página `/casos/[slug].astro` → `PgCasoDetalle` siguiendo doc 02 §2 en 8 secciones numeradas administrativamente: resumen ejecutivo, estado procesal, personas implicadas (agrupadas por grupo procesal con aviso de presunción de inocencia), organizaciones implicadas, cronología, hechos por estado epistémico (4 grupos: acreditado · investigado/atribuido · exculpatorio · desmentido/no_concluyente), biblioteca documental con ancla por documento, y "cómo se ha redactado". Cierra con disclaimer.
- (Sin swimlane gráfico de trayectoria — queda para Fase 2; el flujo cronológico de roles se muestra como tabla en la ficha individual de Persona).

**Lote 4 — Resto de páginas + estructura /cat/ paralela** ✅
- [x] `/personas` (listado tabular con filtros texto + figura pública), `/personas/[slug]` (ficha entity-mast con iniciales en cuadrado navy, biografía, casos donde aparece, trayectoria procesal completa, meta).
- [x] `/organizaciones` (listado tabular con filtros texto + tipo dinámico), `/organizaciones/[slug]` (ficha entity-mast con OrgGlyph 64px, descripción, casos, roles asumidos, documentos producidos).
- [x] `/biblioteca` con todos los documentos del inventario, filtros texto + nivel + tipo, orden nivel↑ · fecha↓, count "citado en N hechos".
- [x] `/sobre` con misión, 6 principios editoriales, escala N1-N4, cómo rectificar, licencias.
- [x] Estructura `/src/pages/cat/` paralela con `.gitkeep` para todas las rutas.

**Lote 5 — Validación final + Pagefind** ✅
- [x] Pagefind 1.5.2 integrado. `data-pagefind-body` en `<main>` del BaseLayout para indexar sólo contenido principal. Build pasa a `astro build && pagefind --site dist`. UI Default en `/buscar` con script inline (Pagefind UI no es ESM), traducciones a castellano, overrides CSS alineados con paleta. Icono lupa en header con label "Buscar" (colapsa a solo lupa en mobile). 17 páginas, 682 palabras indexadas en 0.02s. Búsqueda probada con "plus ultra" → 15 resultados con Plus Ultra Líneas Aéreas en primer puesto.
- [x] Revisión visual desktop + mobile (resize 360px) — header colapsa a 2 filas con search/lang en la primera, todas las páginas legibles, filtros apilan vertical correctamente.

**Catálogo de delitos — entregado en sesión 5c (2026-05-22) ✅**

- [x] `/delitos` (listado tabular con filtros texto + familia) y `/delitos/[slug]` (ficha en 4 secciones: marco normativo, descripción, casos donde se atribuye, atribuciones procesales). Patrón de organizaciones.
- [x] `familiaDelitoLabel()` en `lib/labels.ts` para los 8 valores del enum.
- [x] Nav "Delitos" cableado en `BaseLayout.astro` entre Organizaciones y Biblioteca; estructura paralela `/cat/delitos/` con `.gitkeep`.
- [x] Flag `enableDelitos: true` en `RichProse.astro`. Verificado con 20 enlaces a `/delitos/...` autodetectados en la ficha de Begoña Gómez.

**Asuntos visuales pendientes (no bloquean siguientes fases)**
- [ ] Reemplazar `/branding/wordmark.svg` (y la versión `-inverso`) por SVGs con paths outlined (font-independent), para que el wordmark sea usable como vector en favicon, social cards o impresos. El placeholder actual (`<text>presuntamente</text>` sin font-family) solo funciona si el navegador tiene la fuente del sistema — no fiable.
- [x] Subtítulo institucional del header oculto en mobile (<720px). Confirmado en sesión 5c (2026-05-22): el escudo a 36px + wordmark a 17px funcionan sin él, y el subtítulo recortaba la línea brand. Decisión final: oculto en mobile.
- [x] **Repaso visual mobile (360px) — fix de desbordamiento de página.** Detectado el 2026-05-22 sesión 5: el grid del `.site-header__inner` usaba `1fr auto auto` que se resolvía al min-content del brand (~288px), forzando el contenedor a 426px y haciendo que toda la página scrollee horizontal. Solución: `minmax(0, 1fr)` + `min-width: 0` en `.site-brand`. Wordmark e isotipo reducidos en mobile (17px / 36px en <720, 16px en <400). Las 7 `.tbl` densas (PgCasos / PgPersonas / PgOrganizaciones / PgBiblioteca / PgPersonaDetalle / PgOrganizacionDetalle / PgRectificar) envueltas en `<div class="tbl-scroll">` con `overflow-x: auto`: el scroll horizontal queda contenido en la tabla, no en la página. Patrón reusable para futuras tablas densas.
- [x] **Animación slide-down del panel mobile + nav 2 filas más grande (2026-05-22 sesión 5g).** El panel deja de usar `hidden` HTML (que aplica `display:none` y no anima) y se controla con `data-open` + CSS `max-height` + `opacity` + `visibility`. Apertura ~320ms con ease-out personalizado, cierre ~280ms. `visibility` se retrasa al final del cierre para no cortar la animación; al abrir es instantánea. `prefers-reduced-motion: reduce` deshabilita la transición. Para a11y se sincroniza `aria-hidden`. Adicionalmente: en el rango 720-1080 (header 2 filas) la fila inferior pasa de `padding: 14px; font-size: 13px` a `padding: 18px; font-size: 14px` por feedback del maintainer ("la fila de abajo sigue súper pequeña") — la fila inferior ahora tiene presencia visual equivalente al brand block.
- [x] **Menú hamburguesa en mobile <720px (2026-05-22 sesión 5f).** Por feedback del maintainer ("a partir de cierta estrechez, debería convertirse en la barrita esa de menú al lado del buscador"). Botón ☰ junto a la lupa abre panel desplegable navy con los 6 links verticales y separador con ES/CAT al final (icon también muta a ✕ cuando está abierto). Implementado en `BaseLayout.astro` con vanilla JS por event-delegation desde document (sobrevive a swaps de `ClientRouter`). Cerrado por click en botón / ESC / click fuera / link clicado / `astro:before-preparation`. `body.has-menu-open` bloquea scroll del fondo. Verificado a 360 y 720: aria-expanded sincronizado, panel hidden gestionado, navegación a /delitos cierra el panel y resalta "Delitos" en la siguiente apertura. Esto revisa la decisión "Sin hamburger" anterior. Los rangos finales son: ≥1080 desktop full · 720-1080 colapsado a 2 filas · <720 hamburguesa.
- [x] **Hover/active del nav con subrayado pegado al texto (2026-05-22 sesión 5e).** Antes `.site-nav { align-items: stretch }` estiraba los `<a>` a 78px y el `::after { bottom: 0 }` caía a la base del header junto a la barra tricolor — visualmente desconectado del texto. Cambio a `align-items: center` + padding vertical 12px en el `<a>` (altura natural ~37px) + `::after { bottom: 4px; height: 2px }`. Réplica del patrón de `.site-lang` y mejora ministerial. Adicionalmente: nav colapsado pasa de `height: 40px; font-size: 12px` a `padding: 14px; font-size: 13px` (mismo que desktop) para evitar sensación apretada.
- [x] **Hover full-height del nav + brand más limpio + logo transparente (2026-05-22 sesión 5h).** Tres ajustes finos del header: (a) el cambio de 5e dejaba el hover-background recortado al ~37px del `<a>` (antes 78px de altura completa) porque el padding vivía en el `<a>`. Refactor: el texto del nav se envuelve en `.site-nav__label` y el subrayado vive ahí, no en el `<a>`. Así el `<a>` recupera `align-items: stretch` (full-height del header en desktop, altura natural en colapsado) y el subrayado queda relativo al texto a 4px del baseline. (b) El `::after` del `.site-brand` (filete mostaza decorativo al 35% bajo escudo+wordmark, "placa sellada") se elimina — brand block más limpio, decoración concentrada en banda navy + trim tricolor. (c) `branding/logo.png` con fondo eliminado (transparente, encaja sobre cualquier fondo). Adicionalmente en el hero: kicker pasa de "Sin ruido, sin spin, sin política" a "sin ruido, sin ideología, sin financiación" (gira el énfasis al modelo de sostenimiento del proyecto).
- [x] **Header responsive arreglado en zona intermedia (2026-05-22 sesión 5d).** Dos problemas detectados: (a) entre ~1080px y ~720px el header desktop tipo grid `auto 1fr auto auto` desbordaba porque el nav con 6 items (Casos · Personas · Organizaciones · Delitos · Biblioteca · Sobre, ~513px) no comprimía dentro del `1fr` y empujaba search/lang fuera del viewport — toda la página adquiría scroll horizontal. (b) En mobile el `.site-nav` usaba `overflow-x: auto` y la barra de scroll bajo el nav resultaba "rarísima" (descripción del maintainer). Solución: subir el breakpoint de colapso del header de 720 a 1080 (umbral en el que el grid desktop deja de caber) y reemplazar el scroll del nav por `flex-wrap: wrap`. El mobile real `<720` queda como sub-bloque mínimo que solo oculta el texto "BUSCAR" (la lupa basta). Verificado a 1440/1280/1100/1024/900/720/500/360px: cero scroll horizontal, nav en 1 línea hasta 500px y wraps a 2 líneas (4+2) a 360px.
- [x] **Sistema de badges rediseñado a 4 familias visuales** (2026-05-22 sesión 5). DESIGN.md §2bis reescrita entera. Familias finales: F1 Nivel (mono+gradiente navy) · F-estado (dot + color, cubre épistemico del Hecho Y rol procesal del lado acusado) · F4 Fase (micro-barra de progreso) · F-función (border-left 4px + glyph + fondo neutro: aparato judicial `§ navy`, acusación civil `‡ azul suave`, categorías de hito `§/◆/¶`). Schema parte `condenado` en `condenado_no_firme` y `condenado_firme`. `RolBadge.astro` centraliza el routing rol→familia. Glyphs en caracteres humanistas (`§`, `‡`, `◆`, `¶`) tras descartar `⚖` y `⚑` por cobertura tipográfica. Verificado en Begoña Gómez (8 sub-tipos de badge coexistiendo sin colisión).
- [x] Badge `--phase-archivo` propio (gris desaturado, peso normal, distinto de firme). `PhaseBadge` y `PgCasos` mapean ahora `archivo_provisional` y `archivo_libre` a este bucket con labels distinguibles. Filtro de fase del catálogo añade opción "Archivado".
- [ ] Swimlane gráfico de trayectoria por persona en `PgPersonaDetalle` (hoy se renderiza como tabla cronológica; la versión SVG queda para Fase 2 según doc 02 §3.1).
- [x] Componente `Contraposicion` preparado (dos columnas equivalentes con actor + enunciado + fuentes, sin tratamiento visual que sugiera ganador). Mobile: las columnas apilan vertical con separador horizontal. Disponible para usarse en `PgCasoDetalle` cuando entre un primer hecho con `contraposicion_a`.
- [x] `Money` (chip 1px navy, mono, surface-muted) y `Acronym` (con lookup automático contra collection `organizaciones`, link interno si existe el slug; tooltip sin link si no — nunca link a Wikipedia) preparados. Disponibles para reescribir enunciados de hechos con citación inline.
- [x] `RichProse` v0 (lib + Astro component) auto-detecta acrónimos institucionales (UDEF, AN, JCI, CGPJ, SEPI…) y cifras monetarias en formato español (53 M€, 53,5 millones de euros, 5.000.000 €) sin necesidad de tocar los YAML. Aplicado en Hecho.enunciado, Hito.descripcion, descripcion_corta + resumen_cifras del caso, biografia_corta de Persona, descripcion_corta de Organización.

**Sistema de citaciones inline — RichProse v1 ✅**

`RichProse` v1 (entregada en main el 2026-05-22 sesión 5b) cubre:

- [x] **Acortar money largo**. "53 millones de euros" / "5.000.000 €" / "53,5 millones" / "1 millón de euros" → chip canónico `53 M€` / `5 M€` / `53,5 M€` / `1 M€`. `data-money-raw` + `title=` conservan el texto original para tooltip.
- [x] **Diccionario de organizaciones por nombre completo**. `nombre` + `nombres_alternativos` largos (case-sensitive, word-boundary Unicode `\p{L}\p{N}`). "Audiencia Nacional", "Universidad Complutense de Madrid", "Manos Limpias", "Fiscalía Provincial de Madrid", etc. todos enlazan automáticamente. La regex tolera saltos de línea internos del YAML (espacios literales → `\s+`) para que un nombre cruzado por wrap del block scalar `|` siga matcheando.
- [x] **Diccionario de personas**. `nombre_completo` + `nombres_alternativos` enteros, case-sensitive. "Juan Carlos Peinado", "Joaquín Goyache", "Zapatero", "Barrabés" todos enlazan a `/personas/<slug>`. La case-sensitivity elimina los falsos positivos comunes ("zapateros del PSOE" en minúsculas no enlaza; "Zapatero" sí).
- [x] **Diccionario de delitos** activo desde 2026-05-22 (sesión 5c). El flag `enableDelitos` ya está a `true` en `RichProse.astro` porque la página `/delitos/[slug]` existe.
- [x] **Escape hatch** `[[tipo:slug|label]]`. Sintaxis para forzar comportamiento cuando la auto-detección falla. 4 tipos: `org`, `persona`, `delito` (gateado), `€`. Para money, el primer slot es el chip y el label (opcional, tras `|`) es el tooltip. La auto-detección sigue siendo el camino normal.
- [x] **Anti-falsos-positivos**. Exclusiones `excludeOrgIds` / `excludePersonaIds` propagadas desde `PgPersonaDetalle`, `PgOrganizacionDetalle` y `PgCasoDetalle` (resumen ejecutivo + resumen_cifras + cada `<Hecho>`) para que la propia ficha no se autoenlace consigo misma. Titulares, breadcrumbs, nombre_oficial y cabeceras de tabla NO se enrutan por RichProse — son texto plano por construcción, así que están a salvo sin ningún flag.
- [x] **Skill `investigar-caso` actualizada**. SKILL.md §5 reescrita para reflejar los 4 ejes de v1 y documentar la sintaxis del escape hatch.

**Pendientes residuales (no bloqueantes)**:

- [x] **Exclusión en `Hito.descripcion`**. Resuelto en sesión 5b al cerrar la sesión paralela de badges: prop `excludePersonaIds` / `excludeOrgIds` plumbed por `Hito.astro` y propagado desde `PgCasoDetalle`. Verificado en el caso Begoña Gómez (8 hitos, 6 mencionan su nombre, 0 enlazan a `/personas/begona-gomez`).
- [x] **Diccionario de "cosas de interés" no jerárquico** entregado en main como collection `glosario`: schema `/schemas/glosario.schema.json` con 3 categorías (`programa_publico`, `operacion_policial`, `trama_sobrenombre`) + content collection `/content/glosario/<slug>.yaml`. Primera entrada: `fondo-apoyo-solvencia-empresas-estrategicas.yaml` (citado en Plus Ultra). RichProse detecta `label` + `nombres_alternativos` y renderiza con dotted underline + tooltip de `descripcion_breve` (sin link, decisión editorial conservadora — DESIGN.md §4 prohíbe link externo a Wikipedia, y no son páginas del inventario para link interno).
- [x] **Thousands separator en money sub-millón**. Formatter europeo manual en `formatAmount`: `4.743,53 €`, `113.509,32 €`, `1.234 €` ahora renderizan con punto de miles correcto. Independiente de `toLocaleString`.
- [x] **Whitespace en tooltip de money**. Colapsado a un único espacio en `data-money-raw` y `title=` (antes "113.509,32\neuros" arrastraba el salto del block scalar YAML).

### Fase 1 — MVP Plus Ultra
- [x] Completar schemas con todas las propiedades: `hito`, `hecho`, `documento`, `rol-en-caso`, `organizacion`, `relacion-entre-casos`. Cerrados con `additionalProperties: false` + reglas if/then para V-09, V-10, V-11, V-14, V-15 (rama `fase-1/plus-ultra-content`).
- [x] Skill `incorporar-hito` v0 — toma PDF de auto, propone YAML de `Hito` + `Hecho`(s) + `Documento`(s) + cambios en `RolEnCaso`. Guardarraíles del doc 03 §4 explícitos. Marcada explícitamente como v0 iterativa (rama `fase-1/plus-ultra-content`).
- [ ] YAMLs del caso Plus Ultra (en curso, PR1+PR2 entregados, pendientes nuevos hallazgos):
  - [x] `Caso` raíz.
  - [x] 4-8 `Persona` implicadas — **5/4-8 tras PR2** (juez Calama, Zapatero, Julio Martínez Sola, Roberto Roselli, Javier Martínez Martínez). Cuotas cubiertas; nuevos hallazgos se incorporan en PR3+ cuando aparezcan en fuente oficial.
  - [x] 5-10 `Organizacion` — **9 tras PR2**: 7 de PR1 (AN, JCI 4, JI 15 Madrid, Fiscalía Anticorrupción, SEPI, Plus Ultra, Manos Limpias) + 2 medios en PR2 (Infobae, Libertad Digital).
  - [x] 8-15 `RolEnCaso` — **6/8-15 tras PR2** (Calama juez_instructor, Manos Limpias acusacion_popular, Zapatero investigado, Julio Martínez Sola investigado, Roberto Roselli investigado, Javier Martínez Martínez investigado). Cuota baja cubierta; ampliar conforme aparezcan formalmente nuevos delitos atribuidos a los detenidos en autos.
  - [x] 8-15 `Hito` — **4/8-15 tras PR2** (querella Manos Limpias 2025-12-23, operación UDEF 2025-12-11, cambio_organo JI 15 Madrid → JCI 4 AN marzo 2026, auto imputación Zapatero 2026-05-19). Cuota baja cubierta. Pendientes para PR3+: auto íntegro del 19-may si aparece en CENDOJ, denuncia de Plus Ultra contra UDEF del 2026-03-06 (procedimiento secundario).
  - [x] 15-25 `Hecho` — **5/15-25 tras PR2** (préstamo SEPI atribuido, presunta trama tráfico influencias investigada, rescate por cauces irregulares investigado, detenciones ejecutivos atribuido cobertura cruzada, trama organizada Zapatero investigado por auto). Pendiente PR3+: hechos más finos cuando se publique el auto íntegro del 19-may.
  - [x] 10-20 `Documento` — **6/10-20 tras PR2** (querella Manos Limpias, nota SEPI 2021-03, auto JCI 4 19-may, Infobae operación UDEF, Libertad Digital detención, Infobae cambio_organo). Pendientes para PR3+: nota CGPJ del cambio de órgano (subir el hito a N1), comunicado oficial Policía Nacional/Interior de la operación UDEF, auto íntegro de CENDOJ.
- [x] Delitos del catálogo aplicables a Plus Ultra — 5 en PR1 (tráfico de influencias, organización criminal, falsedad documental, blanqueo de capitales, malversación).
- [x] Componentes Astro de la ficha (siguiendo doc 02): `PgCasoDetalle`, encabezado, resumen ejecutivo, estado procesal, personas implicadas (sin micro-swimlane gráfico, ese queda para Fase 2), cronología, hechos clasificados por estado epistémico, biblioteca de documentos, sección "cómo se ha redactado". *(Lote 3 de la integración del design system.)*
- [x] Pagefind integrado y funcionando con el caso indexado. *(Lote 5.)*
- [x] Página principal con listado básico (un caso). *(Lote 1 — fila hardcoded en `/casos`; Lote 2 lo cablea a Content Collections; Lote 3 añade `/casos/plus-ultra` con la ficha completa.)*
- [x] `/aviso-legal` con texto completo (borrador del doc 04 §8). Página `PgAvisoLegal.astro` en 7 secciones: qué es el sitio, presunción de inocencia, categorización de afirmaciones, rectificación (enlaza a `/rectificar`), responsable LSSI (placeholder hasta dominio), tratamiento de datos personales, licencias. Disclaimer final.
- [x] `/rectificar` con instrucciones funcionales (`PgRectificar.astro`). 5 secciones: vías habilitadas (issue recomendado, correo, burofax), información necesaria (5 campos), plazos comprometidos (tabla), tipos de respuesta (5 opciones), otras vías de mejora (sugerencia de fuente). El formulario opcional vía Cloudflare Workers se evaluará en Fase 2 si el flujo issue resulta insuficiente.
- [x] `NOTES.md` del caso Plus Ultra con metadata, decisiones tomadas, referencias *(ya estaba en main desde PR1; queda actualizado en cuanto entren los YAMLs pendientes en PR2)*.

### Después de Fase 1, antes de Fase 2
- [x] Revisar y consolidar `incorporar-hito` con lo aprendido al fichar Plus Ultra → **v1 en main** con 9 lecciones templatizadas en la sección Histórico (PR1 + PR2).
- [x] Crear `investigar-caso` v0 con lo aprendido → **v0 en main** con 8 guardarraíles + flujo de arranque + bautismo retrospectivo Plus Ultra como caso 0.
- [x] Templates de issue (`rectificacion`, `sugerencia-fuente`) en `.github/ISSUE_TEMPLATE/` con `config.yml` que enlaza a LEGAL.md y CONTRIBUTING.md.
- [x] `CODEOWNERS` placeholder con `@davidchicano` como maintainer único hasta entrar contribuyentes externos.
- [x] `LEGAL.md` raíz consolidado como índice corto que delega en `PgAvisoLegal.astro` del sitio (evita drift entre dos textos).
- [x] **Caso Begoña Gómez — PR1 entregado en main.** Primer arranque real con la skill `/investigar-caso` v0. Esqueleto inicial validado y renderizado. Confirma que el modelo `RolEnCaso` con `fecha_fin` + `hito_fin_id` y el par `investigado → desimputado` funcionan en datos reales (rector UCM Joaquín Goyache, 2025-05-16). Pendientes acumulados para PR2 en `content/casos/begona-gomez/NOTES.md`.

### Fase 2 — Crecer a 5-6 casos vivos
- [x] **Begoña Gómez** PR1 (2026-05-22 — primer arranque con la skill `/investigar-caso`).
- [x] **Begoña Gómez** PR2 (2026-05-22 — acusaciones populares completas, hitos de imputación Goyache + anulación AP Madrid + recurso Fiscalía, UCM perjudicada). Sin push.
- [x] **Begoña Gómez** PR3 (2026-05-22 — incorpora Juan José Güemes como persona desimputada + 5 hitos nuevos posteriores al 21-abr-2026 + hito propio de ofrecimiento de personación a la UCM + nuevo tipo de hito `escrito_conclusiones_provisionales` en el schema). Acumulado del caso tras PR3: **6 personas + 15 organizaciones + 22 documentos + 13 hitos + 8 hechos + 17 roles**. PR3 incorpora: Juan José Güemes (presidente del Centro Internacional de Gestión Emprendedora de IE Business School, ex consejero de la Comunidad de Madrid bajo Aguirre) con persona, dos roles (investigado 18-nov-2024 → 16-may-2025 + desimputado 16-may-2025 → vigente) y dos hitos propios (imputación + recurso Fiscalía contra la imputación 03-dic-2024); el hito de desimputación de Goyache se amplía al mismo auto que desimputa a Güemes; tres hitos posteriores al 21-abr-2026 (ofrecimiento de personación a la UCM por el JI 41 el 03-oct-2025; escrito de conclusiones provisionales de la defensa el 18-may-2026 con más de 30 testigos propuestos y petición de absolución; desestimación del recurso de reforma de la defensa el 22-may-2026); seis documentos nuevos (Newtral imputación Güemes; TheObjective recurso Fiscalía contra imputación Güemes; El Independiente desimputación Goyache+Güemes 16-may-2025; Infobae escrito conclusiones defensa; El Español lista 30+ testigos; Libertad Digital desestimación recurso defensa); una organización nueva (El Independiente). Correcciones: documento del auto de la AP Madrid de desimputación cambia `fecha_documento` a `2025-05-13` (fecha real del auto según cita literal de El Independiente), `fecha_publicacion` a `2025-05-16`; hito y hecho de desimputación reescritos para incluir a Güemes y la exclusión del procedimiento de la contratación de Begoña Gómez por el IE en 2018; rol `ucm-perjudicada` apunta ahora al hito propio. **Cambio de schema en `hito.schema.json`** para añadir el nuevo `tipo: escrito_conclusiones_provisionales` (evento procesal puntual consistente con el patrón existente de `comparecencia_congreso` / `publicacion_investigacion_periodistica`). `pnpm validate` 166 OK, `pnpm build` 77 páginas (subió de 54 a 77 con +1 persona Güemes + 1 org El Independiente + 5 hitos + 1 hecho + 2 roles + 6 docs + las páginas derivadas), `astro check` 0/0/0, ficha verificada con cronología de 13 hitos.
- [ ] Begoña Gómez PR4: pendientes editoriales acumulados en `content/casos/begona-gomez/NOTES.md`. Bloque del maintainer: los archive.org mirrors (19 documentos N4) — el agente no puede llamar a archive.org desde el sandbox. Otros pendientes: notas CGPJ + autos íntegros en CENDOJ + escrito Fiscalía en `fiscal.es` + hito del 17-may-2026 que eleva la cuantía indiciaria del software a >500.000 € + resolución de la AP Madrid sobre el recurso de la Fiscalía cuando se dicte.
- [x] **González Amador** PR1 (2026-05-22 — sesión paralela del segundo agente, mientras el maintainer cerraba Begoña Gómez PR3). Esqueleto del caso ante el JI nº 19 de Madrid contra Alberto González Amador, pareja de Isabel Díaz Ayuso, por presunto fraude fiscal y falsedad documental sobre la mercantil Maxwell Cremona S.L. (ejercicios 2020 y 2021, perjuicio cuantificado por la AEAT en 350.951 €). Acumulado tras PR1: **9 personas + 10 organizaciones nuevas + 9 documentos + 7 hitos + 5 hechos + 14 roles**. Patrón `investigado → desimputado` reaplicado con el asesor fiscal Javier Gómez Fidalgo (imputado 14-abr-2025 → sobreseído provisional 29-may-2025). Cambio de juez Inmaculada Iglesias → Antonio Viejo (oct-2025 por jubilación de Iglesias). AP Madrid Sección 3ª ratifica el procesamiento el 7-nov-2025. **Pendiente de modelado pleno**: Maxwell Cremona S.L. como persona jurídica investigada (V-11 del schema `rol-en-caso` no admite `procesado` para `sujeto_tipo=organizacion`; modelada en PR1 como organización mencionada en descripción y hechos pero sin rol procesal formal; revisión de V-11 análoga a la del `perjudicado` de Begoña Gómez PR2 queda anotada como decisión pendiente).
- [x] **González Amador** PR2 (2026-05-22 — misma sesión paralela). Profundiza con la pieza separada por presunta corrupción en los negocios y administración desleal por operaciones facturadas a Quirón Prevención S.L. en 2020 por importe próximo a 1,9 millones de euros. Incorpora la detención de David Herrera Lobato en Arahal por orden de la AN Sección 2ª (20-ene-2026) como hito de imputación de esa pieza; nueva organización Quirón Prevención S.L.U.; hecho atribuido sobre el escrito de defensa del 19-feb-2026 invocando la condena del FGE García Ortiz como vulneración del proceso justo; tres documentos nuevos.
- [x] **González Amador** PR3 (2026-05-22 — sesión paralela, commit único conforme a la nueva norma de granularidad). **Amplía V-11 del schema** `rol-en-caso` para admitir organizaciones en roles imputadores conforme a la LO 5/2010 (paralela a la ampliación con `perjudicado` aplicada en Begoña Gómez PR2). **Sube a N1 fuentes del BOE**: nombramiento de Antonio Viejo Llorente (Real Decreto 838/2025, de 22 de septiembre, BOE-A-2025-19789) y jubilación de María Inmaculada Iglesias Sánchez (Acuerdo CGPJ del 13-may-2025, BOE-A-2025-16497). **Corrige PR1/PR2** con esos datos oficiales: nombres completos, fecha de jubilación al 10-ago-2025, fecha del cambio de juez al 22-sept-2025, pieza separada Quirón confirmada como instruida por el JI 19 (no por la AN; la AN sólo emitió la orden de detención). **Nuevos elementos**: hito de encomienda a la UCO del 27-jun-2025 (`informe_organismo_publico`); hechos sobre la presunta comisión encubierta de 500.000 € vinculada a Quirón Prevención y sobre el informe UCO pendiente diez meses después; rol Maxwell Cremona procesada como persona jurídica; delito administracion-desleal (art. 252 CP) catalogado; org Público.es. Total acumulado tras PR3: 9 personas + 11 orgs nuevas + 16 documentos (de los cuales 2 N1 BOE) + 9 hitos + 9 hechos + 15 roles. 204 YAML validados, `astro check` 0/0/0. Sin push.
- [ ] **González Amador** PR4+: pendientes acumulados en `content/casos/gonzalez-amador/NOTES.md`. Archive.org mirrors (el maintainer está automatizándolo). Autos íntegros del JI 19 no localizables públicamente (lección reaplicada de Plus Ultra y Begoña: CENDOJ no publica autos de instrucción de Juzgados ordinarios; sólo lo que sube a AP o TS); el auto de la AP Madrid del 7-nov-2025 sí podría aparecer y queda como pendiente prioritario. Escritos formales de personación, calificación y acusación: restringidos a las partes (art. 234 LOPJ / 301 LECrim). Informe UCO cuando se entregue. Carlos Neira al arrancar el caso del Fiscal General y `RelacionEntreCasos`.
- [ ] Koldo · Cerdán · Fiscal General (orden por relevancia objetiva, sin cuota política — Koldo reservado para cuando Begoña Gómez cierre).

---

## Decisiones pendientes del maintainer

Sólo cosas que requieren input humano, no del asistente.

- *(ninguna activa hoy — las tres del 2026-05-21 se resolvieron en el mismo PR1: Zapatero incorporado conforme al auto del 19 may 2026, lista blanca ampliada con sepi.es y otros organismos oficiales, auto del 2026-05-19 citado vía la nota CGPJ en poderjudicial.es).*

---

## Fases del proyecto (estado consolidado)

Detalle en [`docs/diseno/06-roadmap-por-fases.md`](docs/diseno/06-roadmap-por-fases.md).

| Fase | Estado | Comentario |
|------|--------|-----------|
| 0 — Scaffolding | ✅ | Astro 5 + Open Props + schemas + CI + AGENTS.md. Commits `068cc9d`, `a3e429e`, `e1881e5`. |
| 1 — MVP Plus Ultra | 🔄 en preparación | Ver backlog arriba. Skills `incorporar-hito` se construye iterativamente con el caso. |
| 2 — Crecer a 5-6 casos vivos | ⏳ | Begoña Gómez, Koldo, Cerdán, González Amador, Fiscal General. |
| 3 — Visualización + watchers | ⏳ | Mini-grafo, watchers CENDOJ/CGPJ, panel admin. |
| 4 — Apertura editorial | ⏳ | CODEOWNERS reales, contributors externos. |
| 5 — Evoluciones futuras | ⏳ | UI web contributiva, multiidioma, API pública, asociación legal. |

---

## Aprendizajes y notas (vivo)

Cosas que aprendemos por el camino y conviene recordar más allá de los docs de diseño.

- **Las skills se moldean con la experiencia.** No fijar las skills upfront; iterarlas mientras se investiga cada caso. Cada nuevo caso refina la skill correspondiente.
- **Branding lo hace Claude Design** (plataforma de Anthropic, separada de Claude Code). El lenguaje visual canónico vive en [`/DESIGN.md`](DESIGN.md) en raíz; Claude Design lo lee del repo conectado, lo traduce a un bundle (tokens + componentes + UI kit), y Claude Code consume el bundle vía el Handoff. **El Handoff ya llegó** el 2026-05-21 e importado a [`/.claude/skills/presuntamente-design/`](.claude/skills/presuntamente-design/). La skill es `user-invocable: true`, se invoca con `/presuntamente-design` desde futuras sesiones. **DESIGN.md prevalece** sobre cualquier doc del bundle en caso de conflicto (lo dice el SKILL.md del bundle explícitamente).
- **El maintainer no quiere revisar docs largos por defecto.** Resumir, decidir, preguntar sólo cuando es genuinamente bloqueante.
- **`additionalProperties: true` en schemas skeleton es intencional**; se cierra a `false` cuando la propiedad final del schema se conozca al fichar Plus Ultra.
- **Versión de pnpm solo en `package.json`** (`packageManager`). No duplicar en `.github/workflows/`; `pnpm/action-setup` la lee del package.json automáticamente. Duplicar dispara `ERR_PNPM_BAD_PM_VERSION` y rompe CI.
- **pnpm 11 cambia settings de sitio**: `onlyBuiltDependencies` (entre otros) se renombra a `allowBuilds` y vive en `pnpm-workspace.yaml`, no en el campo `pnpm` de package.json (que ahora se ignora). Vale aunque el proyecto no sea monorepo. Ver https://pnpm.io/settings.
- **pnpm 11 bloquea install scripts por defecto** (medida contra supply-chain attacks). Aprobar las deps necesarias en `pnpm-workspace.yaml` → `allowBuilds`. Para este proyecto: `esbuild` y `sharp` (ambas indirectas vía Astro).
- **pnpm 11 requiere Node ≥ 22**. Por eso `.nvmrc` está en `24` y la CI usa `node-version: 24` en `setup-node`.
- **Pendiente futuro próximo**: las actions `actions/checkout@v4`, `pnpm/action-setup@v4`, `actions/setup-node@v4` corren INTERNAMENTE sobre Node 20, deprecadas el 2 jun 2026 y retiradas el 16 sept 2026. Bumpear a `@v5` cuando salga estable de cada una. (El `node-version` del runner ya está en 24; esto sólo afecta al runtime interno de los propios actions.)
- **Tensión brief vs realidad procesal en casos vivos.** Al fichar Plus Ultra el 2026-05-21 surgió la primera tensión entre lo que decía el brief (sin Zapatero como investigado) y la realidad procesal verificable (auto del 2026-05-19 que sí le imputa). Decisión operativa: respetar el brief, dejar la discrepancia documentada en `NOTES.md` del caso + `ROADMAP.md → Decisiones pendientes`, y deferir al maintainer. Reutilizar el patrón en futuros casos donde brief y fuente diverjan: registrar primero, no improvisar.
- **La V-04 no encaja con hechos administrativos no controvertidos** (ej. un Real Decreto en BOE, una nota institucional sobre una decisión del Consejo de Ministros). Al no haber sentencia firme, no pueden ser `acreditado` aunque sean factualmente verificables. Se modelan como `atribuido` con el organismo emisor como actor en el enunciado. Funciona pero sugiere que en Fase 2 conviene revisar si el modelo necesita una etiqueta extra para "factual no controvertido" o si `atribuido` cubre suficientemente.
- **`incorporar-hito` se queda en v0 deliberadamente.** Sólo documenta los guardarraíles y el flujo; tras cada PDF real, ampliar la sección "Histórico" del SKILL.md con lecciones aprendidas. No anticipar plantillas hasta tener uso real.
- **`preview/comp_*.html` son los componentes canónicos del bundle, NO `ui_kits/web/`.** El UI kit web es un prototipo clickable de las páginas (Home/Ficha/...), útil para entender flujo y patrones agregados; pero cada componente individual (header ministerial, footer, badges, hechos, contraposición, etc.) tiene su versión canónica en `/.claude/skills/presuntamente-design/preview/comp_<nombre>.html`. Detalles que sólo viven en `preview/` y que el UI kit web simplifica: doble trim navy+mostaza del header con linear-gradient, trim interno tipo "placa sellada" del bloque blanco, nav/lang con subrayado mostaza animado, filete tricolor administrativo `#8e2424`/mostaza/`#8e2424` del footer con eco al 4-5%. **Regla operativa**: si un componente está en ambos sitios, mirar primero `preview/comp_*.html`; si no está, recurrir al UI kit.
- **El `#8e2424` del filete del footer está permitido.** DESIGN.md prohíbe el rojo SÓLO en estados epistémicos (acreditado/investigado/desmentido) por motivos semánticos (convención UI "rojo=error", `desmentido en rojo` invertiría el mensaje, `investigado en rojo` pre-juzga). El filete decorativo del footer es contexto distinto: la propia DESIGN.md §"Sobre asociaciones partidarias" autoriza usar cualquier tono *siempre que sea muted/institucional*. Como `#8e2424` es rojo BOE apagado (no el rojo PSOE/IU saturado de campaña), encaja con la regla unificadora.
- **`/branding/wordmark.svg` es un placeholder, no un wordmark vectorizado.** Su contenido es `<text>presuntamente</text>` sin `font-family` ni `fill`, así que metido en `<img>` queda con la fuente por defecto del navegador y dimensiones impredecibles. Al integrar el design system se decidió renderizar el wordmark del header como `<span>` estilado (siguiendo `preview/comp_header.html`), y dejar el SVG en `/branding/` para el futuro caso de querer un wordmark vector real (favicon, social cards). **Pendiente**: reemplazar por SVG con paths outlined.
- **HMR de Astro 5 no propaga cambios al `<style>` scoped de componentes `.astro` tras edits sucesivos.** Síntoma: el CSS scoped del navegador queda en la versión anterior aunque el archivo fuente esté actualizado. Solución operativa: reiniciar el dev server (`preview_stop` + `preview_start`, o `pnpm dev` en local). Trivial pero fácil de pasar por alto y produce sesiones de debug innecesarias.
- **Astro 5 `trailingSlash: 'never'` + `build.format: 'directory'`.** Los hrefs internos deben emitirse SIN trailing slash (`/casos`, no `/casos/`). El build sigue generando `dist/casos/index.html` por el `format: 'directory'`, así que las dos cosas conviven sin redirects extra. Si en algún sitio se cuela un trailing slash en un `<a href>`, Astro redirige en runtime — funcional pero ruidoso.
- **Convención de navegación ministerial.** El brand block del header es el ancla a `/`; el nav lista SECCIONES (Casos, Personas, Organizaciones, Biblioteca, Sobre) y NO incluye una pestaña "Inicio". Patrón observado en BOE, Sede Electrónica AEAT, antiguos portales ministeriales — y aplicado en `preview/comp_header.html`. `activeNav` en `BaseLayout.astro` usa la clave `casos` para `/casos`, no `home`.
- **Astro Content Collections con YAML — el `parser` no es público.** Astro 5.18 detecta automáticamente el parser por extensión (`.yaml`/`.yml` → `yaml`), pero la opción `parser` no está expuesta en `GlobOptions` y disparar `astro check` con ella da `ts(2769) No overload matches this call`. `astro build` sí compila (porque no hay typecheck), pero rompe el check. Solución: no pasar `parser`; confiar en la detección por extensión.
- **`generateId: ({ data }) => String(data.id)` para collections anidadas.** Para `hitos`/`hechos`/`roles` (globs `casos/*/{hitos,hechos,roles}/*.yaml`) el id por defecto del loader es la ruta relativa (`plus-ultra/hitos/auto-imputacion-zapatero-2026-05-19`), feo y dependiente del path. Derivar el id del campo `data.id` del YAML hace que `getEntry('hitos', 'auto-imputacion-zapatero-2026-05-19')` funcione naturalmente. Asume unicidad de `data.id` a través de todos los casos (cierto por convención de slugs `<caso>-<tipo>-<fecha>`).
- **Zod schemas mínimos viables con `.passthrough()`.** En vez de duplicar los 9 JSON Schemas de `/schemas/` (que siguen siendo la validación canónica vía `pnpm validate`), los Zod schemas de `content.config.ts` sólo declaran los campos que las páginas consumen hoy. `.passthrough()` evita que Zod elimine campos no listados, así que los YAML pueden tener atributos extra sin romper el typecheck. División de responsabilidades clara: AJV/JSON Schema valida estructura editorial completa (V-01..V-21), Zod sólo aporta tipos a Astro en build.
- **Mapeo `fase_actual` (10 valores) → `phaseKind` UI (3 buckets).** El badge de fase del global.css sólo tiene 3 variantes (`instr`/`juicio`/`firme`). El mapeo actual en `PgCasos.astro` agrupa: instrucción/fase intermedia/denuncia → `instr`; juicio oral/sentencia 1ª inst./recurso → `juicio`; sentencia firme/ejecución → `firme`; archivo → `firme` con label "Archivado". Cuando entren casos archivados al inventario conviene añadir un badge `--phase-archivo` propio (gris desaturado distinto al firme) para evitar confusión visual entre "cerrado con sentencia firme" y "archivado sin condena".
- **"Implicados" = personas con rol de imputación activo, no todo agente del procedimiento.** Decisión editorial razonada en `PgCasos.astro`: la columna "Implic." cuenta personas con `sujeto_tipo='persona'` y `rol ∈ {investigado, procesado, acusado, condenado}` sin `fecha_fin`. Sin esa restricción Plus Ultra daría 2 (Zapatero + Calama, juez instructor) en vez de 1, divergiendo de la cultura periodística española donde "implicado" = imputado, no = juez/fiscal/acusación popular. Reutilizar el patrón cuando entren más casos.
- **Iniciales españolas: primera + última palabra significativa, no las dos primeras.** El helper `iniciales(nombre)` en `lib/labels.ts` toma `palabra[0][0] + palabra[última][0]`. Para nombres compuestos típicos ("José Luis Rodríguez Zapatero", "José Luis Calama Teixeira") esto da JZ vs JT — distinguibles. Tomar las dos primeras palabras daría JL para ambos, perdiendo la diferenciación visual de la card. Stop-words simples (`de`, `del`, `la`, `el`, `los`, `las`, `y`, `i`, `da`, `do`, `van`, `von`) se descartan antes del cálculo.
- **`lib/labels.ts` centraliza mappings enum → label** para roles, tipos de hito/organización/documento, fases y origen. Firma con `_lang` parámetro inicial, así el día que se active el catalán solo hay que añadir la rama. Las páginas no deberían inline-handle estos mappings; pasar siempre por `lib/labels.ts`.
- **Pagefind UI 1.5 no es ESM.** El bundle `pagefind-ui.js` expone `window.PagefindUI` como global, no como módulo. `await import('/pagefind/pagefind-ui.js')` no funciona — el módulo no exporta nada y aunque carga, `mod.PagefindUI` es undefined. Solución: `<script src="/pagefind/pagefind-ui.js" is:inline></script>` (script tag normal) seguido de otro script que llame a `new PagefindUI({...})` en `DOMContentLoaded`. La docs oficial usa exactamente este patrón.
- **Pagefind necesita un build previo para servir su índice; `pnpm dev` no lo construye.** El script `build` ejecuta `astro build && pagefind --site dist`; el dev server (`astro dev`) no construye `dist/` ni el índice. Resultado práctico: `/buscar` funciona en `pnpm preview` o en producción, pero no en `pnpm dev`. La página detecta la ausencia con `typeof PagefindUI === 'undefined'` y muestra un fallback con instrucciones. Existe un script paralelo `build:no-index` (sólo `astro build`) por si en algún momento se quiere construir sin indexar.
- **`data-pagefind-body` en `<main>` para excluir chrome.** Sin el marcador, Pagefind indexa todo el HTML — header, sub-strip, footer — y los resultados aparecen plagados de "presuntamente.org · Inventario público…" repetido. Con el marcador en `<main>`, Pagefind reporta "Ignoring pages without this tag" + indexa sólo el contenido principal. Para `/buscar` se añade `data-pagefind-ignore` al `<div id="pagefind-search">` para que los resultados que renderiza la propia UI no se indexen en pasadas posteriores.
- **PersonaCard vs OrgCard — la distinción visual obligatoria es iniciales-en-cuadrado vs glyph-geométrico.** DESIGN.md §4 lo declara duro: Persona = iniciales, Organización = símbolo por tipo. Implementado en los componentes con borde-izquierdo coloreado distinto (navy para persona, mostaza para org) además del glyph diferente, para que las cards en una sección mixta (p.ej. "personas y organizaciones implicadas" lado a lado en la ficha de caso) sean discernibles incluso en escaneo rápido.
- **El badge epistémico mapea 6 valores a 4 estilos.** `EpistemicBadge` reusa la clase `--investigado` para `atribuido` (mismo color amarillo apagado, label distinto) y `--desmentido` para `no_concluyente`. Razón: el sistema solo tiene 4 colores epistémicos en la paleta (acreditado/investigado/exculpatorio/desmentido). Las dos categorías extra del modelo son semánticamente cercanas a las existentes; introducir colores nuevos confundiría más que ayudaría. Si el modelo evoluciona en Fase 2 con una distinción visual real (p.ej. "factual no controvertido"), revisar.
- **`getStaticPaths()` con params = entry.id.** Astro 5 rutas dinámicas `[slug].astro` usan `getStaticPaths` que devuelve `{ params: { slug }, props }`. Como nuestros `generateId` ya devuelven `data.id`, basta con `caso.id` como slug — coincide naturalmente con el slug humano del YAML. Patrón reusado en `/casos/[slug]`, `/personas/[slug]`, `/organizaciones/[slug]`.
- **El par `investigado → desimputado` con `fecha_fin` + `hito_fin_id` funciona.** Validado por primera vez en el caso Begoña Gómez (PR1) con el rector UCM Joaquín Goyache: se modela como dos `RolEnCaso` consecutivos del mismo sujeto (uno `rol=investigado` con `fecha_fin` + `hito_fin_id`, otro `rol=desimputado` con `fecha_inicio` igual a la `fecha_fin` del anterior). La ficha del caso renderiza correctamente la trayectoria en la sección "ABSUELTOS / DESIMPUTADOS" del bloque de personas implicadas, y la card de Persona muestra los dos roles consecutivos en una micro-tabla. Confirma que el modelo es expresivo para casos vivos con trayectorias procesales no monolíticas. **Patrón reusable** en todos los casos con sobreseimientos individuales: una desimputación es un cierre de rol + apertura de otro, no una modificación destructiva del rol previo.
- **Sobreseimientos parciales de delito (no de procedimiento) se modelan como `Hecho(tipo=exculpatorio)`, no como `Hito` separado.** El auto del 2026-04-13 archiva el delito de intrusismo profesional contra Begoña Gómez manteniendo los otros cuatro delitos. El procedimiento continúa, así que no aplica `Hito(tipo=archivo_provisional)` ni `Hito(tipo=sobreseimiento_libre)` (que sugerirían cierre total). Se modela como un `Hecho(tipo=exculpatorio)` colgado del hito principal (auto procesamiento), citando el pasaje específico del auto que archiva ese delito. Patrón reusable cuando un auto procesa por unos delitos y archiva otros simultáneamente. (Si en el futuro un auto archiva la pieza entera contra una persona dejando vivas otras piezas, sí aplicaría `Hito(tipo=sobreseimiento_libre)` para esa pieza.)
- **El badge `--phase-intermedia` se introdujo en la sesión 5 del rediseño de badges** para distinguir el procesado de la instrucción activa. Mismo accent navy que `instr` y `juicio`, pero con 2 de 4 segmentos pintados en la micro-barra de progreso. Mapeo cerrado en `PhaseBadge.astro` (`fase_intermedia` → `intermedia`, label "Fase intermedia") y el filtro del catálogo (`PgCasos.astro`) lo ofrece como opción. Verificado en Begoña Gómez tras el procesamiento del 2026-04-13.
- **Documentos N1 sin URL canónica localizable se modelan como N4 con cobertura cruzada.** Ya aprendido en Plus Ultra PR2 con la operación UDEF y el cambio_organo; reaplicado en Begoña Gómez PR1 para el auto de procesamiento del JI nº 41 de Madrid (2026-04-13) y para el auto de desimputación de la AP Madrid (2025-05-16). En ambos casos el documento existe (es citable, fechado, autor identificado) pero no aparece publicado con URL canónica ni en `poderjudicial.es` ni en CENDOJ; el `nivel_fuente` se baja a 4 y se justifica con cruce mediático cumpliendo V-13. Cuando aparezca la URL canónica oficial, se sube el nivel y se mantiene el `id` del documento. La sustitución no rompe los hechos derivados.
- **V-11 estaba mal calibrada: organizaciones también pueden ser `perjudicado`.** Descubierto en Begoña Gómez PR2 al fichar la UCM como perjudicada (vista del 06-oct-2025). La regla original limitaba a organizaciones a roles `acusacion_popular / acusacion_particular / querellante / denunciante`. El "perjudicado" es legítimamente parte civil del procedimiento penal español (responsable de la acción civil derivada del delito); excluirlo era una restricción excesiva. Cambio mínimo aplicado a `schemas/rol-en-caso.schema.json` añadiendo `perjudicado` al enum del bloque if/then de V-11. Documentado en el propio schema con referencia al caso real que motivó el cambio. **Patrón**: cuando una validación del schema choca con un dato real verificable y procesalmente legítimo, la opción correcta es revisar la validación, no forzar el dato. La regla V-11 sigue cumpliendo su propósito (evitar que se modelen roles "imputador" sobre organizaciones); sólo se amplía la lista permitida.
- **Documentos N2 (oficial secundaria / instructora) ya tienen su primer ejemplo en el inventario.** El escrito de la Fiscalía Provincial de Madrid del 21-abr-2026 contra el auto de procesamiento de Begoña Gómez se modela como `nivel_fuente: 2` con `tipo: escrito_fiscalia`. La justificación explicita por qué no es N1 (no está en `fiscal.es` con URL canónica) ni N4 (es escrito oficial de un órgano público, no cobertura periodística). Es un nivel intermedio del modelo que Plus Ultra no había usado todavía. Patrón reusable para informes UDEF/UCO filtrados, escritos de Fiscalía no en `fiscal.es`, notas institucionales de órganos públicos fuera de la lista blanca, etc.
- **Acusaciones populares múltiples coetáneas: misma fecha con justificación común.** En Begoña Gómez se modelaron Hazte Oír, Vox e Iustitia Europa con `fecha_inicio: 2024-04-29` (cobertura cruzada "días después" del 24 de abril). MRPE quedó en mayo de 2024 al haber tenido que prestar fianza después. Cuando varias acusaciones populares se personan en bloque tras la apertura del procedimiento, se sincronizan fechas con la mejor estimación cruzada disponible y se anota en las notas del rol que el auto específico de admisión está pendiente. La sincronización es defendible editorialmente (el grupo se comporta procesalmente como bloque inicial) y conservadora (no inventa fechas exactas).
- **El rol procesal `condenado` se separa en `condenado_no_firme` y `condenado_firme`.** Cambio editorial introducido el 2026-05-22 al rediseñar el sistema de badges. Razón: la presunción de inocencia formal solo cae cuando la sentencia es firme; mantener un único `condenado` agnóstico ocultaba esa distinción jurídicamente crítica. El badge UI los pinta distinto (rojo apagado outline vs rojo chillón fill). La V-10 ya pide `hito_origen_id` apuntando al Hito de sentencia correspondiente.
- **El rol `investigado` no debe ir en rojo ni granate**, ni siquiera apagado. Una persona puede ser investigada y resultar totalmente inocente (sin auto judicial de procesamiento de por medio). El navy outline marca "estado base del procedimiento, no acusatorio". El primer salto al "color de gravedad" (mostaza) ocurre en `procesado`, cuando un juez ya ha valorado indicios. Esta es **la frontera editorial real** entre "persona que pasaba por ahí" y "persona con valoración judicial de implicación". Aplicar la misma lógica en futuras fichas y en discusiones de diseño con Claude Design.
- **Glyphs Unicode para badges: pegarse a fuentes humanistas, evitar Misc-Symbols.** El primer diseño usó `⚖` (U+2696) y `⚑` (U+2691) para aparato judicial y acusación civil. Lato/Gill Sans/Source Sans no cubren esos puntos de código y caen a "Apple Symbols" en macOS / "Segoe UI Symbol" en Windows, donde se renderizan irreconocibles a 11-12px. Solución que funcionó: `§` (signo de sección, símbolo jurídico clásico) y `‡` (doble daga, "cita / referencia"), ambos en el bloque Latin-Suplemento / Misc Punctuation, presentes en todas las fuentes humanistas. **Regla operativa**: si un glyph cae fuera del bloque Latin-Suplemento o Misc Punctuation, comprobar antes que se renderiza correctamente en Lato.
- **Coherencia conceptual entre F-estado epistémico (Hecho) y F-estado rol procesal (Persona) usando el mismo dot+color.** Un `Hecho.tipo = investigado` y un `RolEnCaso.rol = investigado` comparten el mismo color ámbar... espera, no: tras el rediseño final el rol `investigado` va en navy, no en ámbar. La coincidencia conceptual subsiste pero por otra vía: en ambos casos el `•` dot a la izquierda indica "estado de algo", y el contexto desambigua (Hecho vs PersonaCard). El navy del rol `investigado` se distingue del navy del aparato judicial porque el aparato judicial usa F-función (border-left + glyph), no F-estado.
- **Sincronización repo ↔ Claude Design es manual y debe pedirse explícitamente.** El bundle de design system en `/.claude/skills/presuntamente-design/` es una copia local generada por Claude Design (plataforma de Anthropic, separada de Claude Code). Cuando se cambia algo sustancial de diseño desde Claude Code (DESIGN.md, tokens de global.css, vocabularios visuales), el bundle queda desactualizado. La convención añadida en SKILL.md de la skill: el agente debe **recordar al maintainer** que pase un prompt a Claude Design para regenerar el bundle. Los prompts se guardan en `/.claude/skills/presuntamente-design/sync-prompts/` por trazabilidad histórica.
- **`\b` ASCII no funciona como boundary con caracteres acentuados ni con `ñ`.** Síntoma típico: `\bBegoña\b` en JavaScript hace match en cualquier posición porque la "o→ñ" interna ya es una transición word↔non-word para el motor ASCII. Solución para RichProse v1: lookaround Unicode `(?<![\p{L}\p{N}])TÉRMINO(?![\p{L}\p{N}])` con flag `u`. Acepta puntuación, espacios, `º`, `·`, paréntesis, comillas, etc. como delimitadores y rechaza letras y dígitos. Patrón reusable para cualquier matching de términos en prosa española.
- **YAML block scalar `|` envuelve nombres largos por saltos de línea, rompiendo regex con espacios literales.** Síntoma en RichProse v1: "Audiencia Nacional" no enlazaba en la `descripcion_corta` de `plus-ultra-lineas-aereas.yaml` porque el wrap del block scalar partía el texto a "Audiencia\nNacional", y mi regex usaba un espacio literal. Solución: en `pushTermCandidates`, convertir cualquier whitespace literal del término en `\s+` en la regex compilada. Trivial pero crítico. Si en el futuro se mete un nombre con tabs u otros whitespace exóticos, el mismo `\s+` los cubre.
- **`toLocaleString('es-ES')` en Node 22 con full ICU no siempre aplica grouping con `useGrouping: true` por defecto.** Confirmado en runtime de Astro dev: `(4743.53).toLocaleString('es-ES', { maximumFractionDigits: 2 })` devuelve `"4743,53"` sin punto de miles. Para amounts ≥ 1M el dato no se ve porque el formato termina en "M€"; para amounts entre 1k y 1M sí afecta visualmente. Cosmético, no funcional. Si en el futuro se quiere forzar grouping, usar formatter explícito en lugar de delegar al locale.
- **`1fr` en grid NO se comprime por debajo del min-content de su hijo.** Es la causa raíz del bug del header en zona 720-1080: `grid-template-columns: auto 1fr auto auto` con un nav que internamente pedía 513px no podía caber en viewports más estrechos, y el inner-grid empujaba search/lang fuera del viewport en lugar de comprimirse. Para que un `1fr` ceda hace falta `minmax(0, 1fr)` (ya aplicado en el bloque mobile del `.site-header__inner`) o un `min-width: 0` en el hijo, pero ninguna de las dos elimina del todo el problema cuando los items del nav son flex con padding fijo y mucho texto. La solución limpia es **bajar a un layout colapsado antes de llegar al límite de min-content**, no intentar comprimir el desktop. Patrón reusable cuando un grid header empieza a romper: subir el threshold de colapso al ancho donde la suma de min-contents excede el viewport.
- **`overflow-x: auto` en un nav horizontal es feo y poco descubrible.** El nav mobile estaba pensado para ser "scrollable si los items no caben" (cita del comentario CSS antiguo), pero en la práctica la barra de scroll asomando bajo la banda navy se ve como un error visual. La alternativa `flex-wrap: wrap` deja que los items pasen a 2 líneas naturalmente y conserva la estética ministerial sin hamburguesa. Regla operativa: prefiere wrap antes que scroll para navegación visible.
- **Plural español -ón/-ones pierde la tilde.** `"atribución" + "es"` da `"atribuciónes"`, incorrecto; el plural es `"atribuciones"` (sin tilde porque la sílaba tónica deja de ser última al pluralizar). Pasa con cualquier palabra aguda terminada en `-ón`/`-és`/`-ín`/`-án`/`-ún`. Evitar concatenar sufijos con plantillas tipo `${count} atribución${count === 1 ? '' : 'es'}` y usar singular/plural explícitos: `count === 1 ? 'atribución' : 'atribuciones'`. Aprendido al hacer la ficha de delito; aplicar el mismo cuidado en futuras pluralizaciones.
- **Un mismo Hito puede afectar a varias personas con el mismo `tipo`.** Confirmado en PR3 del caso Begoña Gómez: el auto de la AP Madrid del 13-may-2025 desimputa **a la vez** al rector de la UCM Joaquín Goyache y al directivo del IE Juan José Güemes. El patrón correcto es un único `Hito(tipo=desimputacion)` con `personas_afectadas: [goyache, guemes]` y dos pares de `RolEnCaso(investigado, fecha_fin) + RolEnCaso(desimputado, fecha_inicio)` para cada persona, todos apuntando al mismo `hito_fin_id` / `hito_origen_id`. La alternativa (un hito por persona afectada) divide artificialmente lo que es un único auto judicial. **Patrón reusable** en cualquier hito jurisdiccional que afecte simultáneamente a múltiples personas (sobreseimiento de varios investigados, procesamiento conjunto, anulación de imputaciones, etc.).
- **El slug del Hito es estable; la `fecha` del Hito puede corregirse sin renombrar.** Distinción aplicada en PR3 del caso Begoña Gómez al actualizar el auto AP Madrid de desimputación: el documento mantiene su id `auto-audiencia-desimputa-goyache-2025-05-16` (slug interno, identificador estable de cara a referencias YAML cruzadas) pero su `fecha_documento` se corrige a `2025-05-13` (fecha real del auto, según cita literal de El Independiente) y se distingue de `fecha_publicacion: 2025-05-16` (cuando se conoce mediáticamente). El slug del hito que lo referencia (`desimputacion-goyache-bg-2025-05-16`) también se conserva. **Patrón**: los slugs son identificadores; los campos `fecha_*` son contenido editorial y se corrigen cuando se obtiene mejor información.
- **El `escrito_conclusiones_provisionales` es un evento procesal puntual modelable como Hito.** Tipo nuevo añadido al enum del schema en PR3 del caso Begoña Gómez. En procedimiento abreviado español, el escrito de conclusiones provisionales de defensa (y de las acusaciones) marca el cierre de la fase de instrucción y el inicio de la fase intermedia previa al juicio oral. Es un evento de parte, no jurisdiccional, pero el enum del schema ya admitía otros eventos de parte (`comparecencia_congreso`, `publicacion_investigacion_periodistica`), así que la adición es consistente. V-14 (hitos jurisdiccionales requieren `documento_principal_id`) no aplica al nuevo tipo por defecto, pero al modelarlo siempre con `documento_principal_id` por convención editorial no hay diferencia práctica.
- **Cobertura mediática internacional / agencia se duplica entre medios.** Al fichar el escrito de defensa del 18-may-2026, la misma noticia de Europa Press / EFE aparecía publicada en El Diario Alerta, El Progreso, Noticias de Navarra, Infobae, Público y otros. Convención editorial al elegir cuál citar como respaldo N4: priorizar el medio nacional más reconocible que tenga URL canónica estable y rastreable (Infobae es buena opción por su archivo abierto; El Español por su redacción propia tribunal). Evitar enlazar a copias regionales o de menor cobertura cuando hay opciones nacionales.

---

## Cómo se mantiene este documento

**Obligatorio** para cualquier agente o maintainer que trabaje en el repo:

1. **Al abrir el proyecto** o empezar una nueva sesión, leer este fichero entero antes de hacer nada.
2. **Al cerrar una sesión** o tras un cambio significativo, actualizarlo:
   - Marcar items completados.
   - Eliminar los completados tras una o dos sesiones (la historia detallada vive en el git log).
   - Añadir nuevos items detectados.
   - Si se ha tomado una decisión arquitectónica, anotarla en *Aprendizajes y notas* y actualizar el doc de diseño correspondiente.
3. **No duplicar contenido de `docs/diseno/`**. Aquí va el "qué" operativo; allí el "por qué" razonado.
4. **Si la convención cambia**, actualizar también `AGENTS.md` §"Workflow para agentes".
